export class wind {

    public speed: number;
    public deg: number;
    public gust: number;

    constructor(speed: number, deg: number, gust: number) {
        this.speed = speed;
        this.deg = deg;
        this.gust = gust
    }
}